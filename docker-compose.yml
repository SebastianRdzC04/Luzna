services:
  # ...existing code...
  frontend-luz:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "2010:80"
    container_name: frontend-luz
    restart: unless-stopped
    # depends_on: [other services if needed]
  # ...existing code...